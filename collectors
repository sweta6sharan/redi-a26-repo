"""
def avg_temp(*args: tuple):
    total = 0.0
    for item in args:
        total += item
    average = total / len(args)
    print(f"Average temperature: {average}")


avg_temp(12, 25, 31, 18, 7, 28)
"""
"""def configure_laptop(**kwargs: dict):
    """Display and return a laptop configuration supplied as keyword arguments."""
    print("Your laptop configuration will be:\n")
    for key, value in kwargs.items():
        print(f"{key}: {value}")
    return kwargs


configure_laptop(
    RAM="16GB",
    CPU="Intel i7",
    Storage="1TB SSD",
    GPU="NVIDIA RTX 3080",
)
"""


def fn(a, /,b, c=3,*pc,e=4, **kc):
    print(f"{a}, {b}, {c},*pc={pc}, e={e}, **kc={kc}")
    fn(0,1,2,3,4,5,f=6,g=7)