def calculate_ticket_price(age,number_of_tickets,student=False):
    if age < 12:
        price_per_ticket = 6
    elif age <= 17:
        price = 8
    else:
        price = 12

    total_price = price_per_ticket * number_of_tickets
    if student and age >= 18:
        total_price *= 0.8  # Apply a 10% discount for students
    return total_price
print(calculate_ticket_price(10, 2))  # Output: 12
print(calculate_ticket_price(15, 3))  # Output: 24